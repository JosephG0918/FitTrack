I created this web app to track my workout progress. Recently, I got into exercising and thought this would make a fun project.

workout-CLI:
Python scripts that interact with the MariaDB database to insert, display, and delete workout data.

workout-webapp:
A web app built with the Flask micro-framework that displays workout data from a MariaDB database hosted on a Raspberry Pi 4 Model B.